
<%= form_for(@control,url: {controller: "control_audits", action: "create"},:method => "post") do |f| %>


<%= render 'form', :f => f,  :control => @control, :control_audit => @control_audit %>

<div class="col-lg-12 col-md-6 col-sm-6 col-xs-12">
                        <div class="btn-group gr-btn save-file">
                          <%= f.submit "Save as Draft", :class => "grey_btn"%>
                          <%= f.submit "File Risk", :class => "orange_btn"%>
                        </div>

<%end%>