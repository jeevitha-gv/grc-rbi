<%= f.label :name, t('other_asset.name') %>
<%= f.text_field :name %>

<%= f.label :manufacturer, t('other_asset.manufacturer')%>
<%= f.text_field :manufacturer%>

<%= f.label :asset_type_id, t('other_asset.asset_type_id')%>
<%= f.collection_select(:asset_type_id, current_company.asset_types, :id, :name) %>

<%= f.label :asset_status_id, t('other_asset.asset_status_id')%>
<%= f.collection_select(:asset_status_id, AssetStatus.all, :id, :name) %>

<%= f.label :model, t('other_asset.model') %>
<%= f.text_field :model %>

<%= f.label :serial_number, t('other_asset.serial_number') %>
<%= f.text_field :serial_number %>

<%= f.label :asset_id, t('other_asset.asset_id') %>
<%= f.text_field :asset_id %>

<%= f.label :ip, t('other_asset.ip') %>
<%= f.text_field :ip %>

<%= f.label :description, t('other_asset.description') %>
<%= f.text_area :description %>

<%= f.label :asset_owner, t('other_asset.owner') %>
<%= f.collection_select(:asset_owner, current_company.users, :id, :user_name) %>

<%= f.label :asset_user, t('other_asset.user') %>
<%= f.collection_select(:asset_user, current_company.users, :id, :user_name) %>

<%= f.label :location_id, t('other_asset.location_id') %>
<%= f.collection_select(:location_id, current_company.locations, :id, :name) %>

<%= f.label :department_id, t('other_asset.department_id') %>
<%= f.collection_select(:department_id, current_company.departments, :id, :name) %>


<%= link_to "Attach Files", other_assets_path%>