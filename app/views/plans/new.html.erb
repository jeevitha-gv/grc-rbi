<<<<<<< HEAD
<div class="container">    
      <!-- Nav tabs -->
     <div class="risk-title">Subscription </div>
     <div class="clearfix"></div>
    	<div class="">
        	<div class="row">
			<% @subscriptions.each do |subscription| %>
        <div class="col-lg-3 col-md-3 col-sm-3 col-xs-12">
            <div class="plan">
                <div class="head">
                    <h2><%= subscription.name%></h2>
                </div>
                <ul class="item-list">
										<li><strong><%= subscription.description%></strong> Description</li>
                    <li><strong><%= subscription.file_size%>MB</strong> Storage</li>
                    <li><strong><%= subscription.user_count%></strong> Sign ups</li>
                    <li><strong><%= subscription.subscription_sections.map(&:name).join(', ')%></strong> Modules </li>
                    <li><strong><%= subscription.valid_log%><%= plan_validity_period(subscription.valid_period)%></strong> Valid period</li>
                    <!--<li>24/7 Email & Phone <strong>support</strong></li>-->
                </ul>
                <div class="price">
                    <h3><%= subscription.amount%></h3>
                    <h4>per month</h4>
                </div>
                <a href="<%= new_company_path(subscription: subscription.name)%>"><button type="button" class="btn btn-warning">Sign Up</button></a>
            </div>
        </div>
        <%end%>
    </div>
		</div>
  </div>
=======
<%= form_for @plan do |f|%>

<ul>
<%@subscriptions.each do |subscription|%>
<li> 
Subscription Name:	
<a href="<%= new_company_path(subscription: subscription.name)%>"><%= subscription.name%></a>
Description:
<%= subscription.description%>
Amount:
<%= subscription.amount%>
Log:
<%= subscription.valid_log%>
Period:
<%= "Month(s)"%>
</li>
<%end%>
</ul>


<%end%>
>>>>>>> 6dc4c289f686c70c378fc32849e090987973a198
